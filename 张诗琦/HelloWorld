import tensorflow as tf
def main(_):
    with tf.Session() as sess:
        welcome = sess.run(tf.constant("Hello, MSC!"))
        print(welcome)
    exit(0)


if __name__ == "__main__":
    tf.app.run()
